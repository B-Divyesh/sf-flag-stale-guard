# Changelog

## Unreleased

- Fail closed when a configured scan path is missing or cannot be read.
- Validate expiry dates as `YYYY-MM-DD` and reject whitespace-only owners.
- Replace the unavailable registry install command with a checkout install.
- Add complete claim, package, action, route, metadata, resize, and privacy regressions.

## 0.1.0

- First release: static flag inventory, expiry metadata checks, literal reference scan, removal gate, JSON output, and bundled demo.
