{
  "name": "flag-stale-guard-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:site",
    "build:site": "vite build",
    "test": "cargo test && npm run test:build-contract && playwright test",
    "test:build-contract": "npm run build:site && node --test scripts/build-contract.test.js",
    "test:cli": "cargo test",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "axe-core": "^4.10.2",
    "vite": "6.4.3"
  }
}
